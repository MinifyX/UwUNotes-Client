{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The setup window: its own commands, plus dragging, minimizing and closing itself. The commands in app.rs are plain #[tauri::command]s and need no permission of their own; no plugin is loaded, so there is nothing else the page could reach.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-close"
  ]
}
